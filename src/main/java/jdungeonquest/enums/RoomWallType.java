package jdungeonquest.enums;

public enum RoomWallType {
    WALL, //wall, no exit
    DOOR, //player must take a Door card upon entering
    EXIT; //a regular type of connection between rooms
}
